pipeline {
    agent any

    stages {
        stage('test ansible plugins') {
            steps {
				ansiblePlaybook credentialsId: 'ansibleuse', inventory: '/etc/ansible/hosts', playbook: '/etc/ansible/my_playbook.yml'           
            }
        }
    }
}