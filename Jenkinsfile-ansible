pipeline {
    agent any

    stages {
        stage('test ansible plugins') {
            steps {
				ansiblePlaybook inventory: '/etc/ansible/inventory/hosts', playbook: '/etc/ansible/playbook.yml'           
            }
        }
    }
}